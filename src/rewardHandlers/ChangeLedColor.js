import BaseHandler from './base/BaseHandler.js';

export default class ChangeLedColor extends BaseHandler {
  // connect to arduino
  // check user input
  // send out number to change color

  handle(user, reward, cost, message, extra) {
    //
  }
}
